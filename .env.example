# Cloudflare 憑證。複製成 .env 後填入；.env 已被 gitignore。
#
# ⚠️ 先讀這段再決定要不要填 token
#
# wrangler 會自動載入「執行目錄下的 .env」——這是 Cloudflare 官方文件推薦的設定方式
# （https://developers.cloudflare.com/workers/wrangler/system-environment-variables/）。
# 副作用是：只要下面那行填了值，它就會【無聲蓋掉】你 `wrangler login` 的 OAuth session。
# 如果那把 token 的權限比 OAuth 窄（官方「Cloudflare Pages」範本產生的就是），
# 你會看到「在這個專案目錄裡認證失敗、在別的目錄一切正常」，非常難聯想到原因。
#
#   本機開發：跑過 `wrangler login` 就【不要】填 CLOUDFLARE_API_TOKEN。
#             其實整份 .env 都可以不用建，make deploy / make counter 直接走 OAuth。
#   CI 等非互動環境：才需要填（那裡沒有瀏覽器可以做 OAuth）。
#
# 留空值是安全的：實測 wrangler 4.90，`CLOUDFLARE_API_TOKEN=`（空字串）會被忽略，
# 仍然使用 OAuth；只有填了真的值才會覆蓋。
#
# Token: https://dash.cloudflare.com/profile/api-tokens
#   make deploy  需要 Account → Cloudflare Pages:Edit（官方「Cloudflare Pages」範本就夠）
#   make counter 另外需要 Account → D1:Edit
#                ↑ Pages 範本【不含】D1，要自己加這一列。少了它 wrangler 只會回
#                  "Authentication error [code: 10000] / incorrect permissions"，
#                  讀起來像是沒登入，其實是權限缺一塊。
CLOUDFLARE_API_TOKEN=
# Account ID: 執行 `npx wrangler whoami`。用 OAuth 時通常不必填。
CLOUDFLARE_ACCOUNT_ID=

# 這裡沒有「Pages 專案名稱」與「輸出目錄」，因為它們不從 .env 讀（設了也不會有任何效果）：
#   專案名稱  ← <課程>/course.config.json 的 site.project，Makefile 的 PROJECT 從那裡取
#   輸出目錄  ← Makefile 的 DIST（預設 dist），要換就 `make DIST=... deploy`


# ══════════════════════════════════════════════════════════════════════════
# 外部資料庫憑證（`make verify` 用）
# ══════════════════════════════════════════════════════════════════════════
#
# 下面六個來源全部免費。**整段都可以留空**——每一個都有降級路徑，缺憑證時
# `make verify` 的行為跟沒有這一段時逐字元相同，只是比較慢、少幾項檢查。
#
# 這不是客氣話，是硬性設計：GitHub Actions 在 **fork 來的 PR** 上拿不到
# repository secrets。任何一支腳本在缺憑證時 exit 1，外部貢獻者的 PR 就會
# 一律紅掉，而且錯誤訊息指向一個他們無權取得的東西。
#
# 要在 CI 用，把同名的值加進 Settings → Secrets and variables → Actions。
# 設計理由見 docs/plans/2026-08-04-external-db-apis-design.md。

# ── NCBI E-utilities（PubMed）─────────────────────────────────────────────
# 效果：3 req/s → 10 req/s。469 筆 PMID 的驗證是這支腳本最慢的一段。
# 缺這把：退回 3 req/s，一切照舊，只是 make verify 久一點。
#
#   1. 開 https://account.ncbi.nlm.nih.gov/settings/
#      （沒帳號就先註冊；可以用 Google／ORCID 等第三方登入，不必另外設密碼）
#   2. 往下捲到「API Key Management」
#   3. 按「Create an API Key」——不必填任何用途說明，按下去就產生
#   4. 複製那串英數字（約 36 字元）貼到下面
#
# 官方速率說明：https://www.ncbi.nlm.nih.gov/books/NBK25497/
NCBI_API_KEY=

# ── YouTube Data API v3 ───────────────────────────────────────────────────
# 效果：verify_links.py 從「打 oEmbed 猜 HTTP 狀態碼」變成直接讀
#       uploadStatus／privacyStatus／embeddable／regionRestriction／duration。
#       oEmbed 分不出「設為私人」與「禁止嵌入」（兩者處置完全不同），也看不到
#       地區封鎖——那正是它的註解裡承認要另外跑 yt-dlp 才知道的事。
# 配額：一次查 50 支影片算 1 unit，日配額 10000。這門課 344 支影片＝7 units。
# 缺這把：退回 oEmbed，跟今天完全一樣。
#
#   1. 開 https://console.cloud.google.com/ 建一個專案（名字隨便，例如 curate-course）
#   2. 開 https://console.cloud.google.com/apis/library/youtube.googleapis.com
#      確認上方選的是剛才那個專案，按「啟用」
#   3. 開 https://console.cloud.google.com/apis/credentials
#      → 「建立憑證」→「API 金鑰」
#   4. 建議接著按「編輯 API 金鑰」→「API 限制」→ 只勾 YouTube Data API v3。
#      這把金鑰會進 CI，限制住它的話外流也只能查公開影片資訊。
#   5. 複製金鑰（AIza 開頭）貼到下面
#
# 不需要開帳單。YouTube Data API 的免費配額不必綁信用卡。
# 配額計算：https://developers.google.com/youtube/v3/determine_quota_cost
YOUTUBE_API_KEY=

# ── Crossref polite pool ──────────────────────────────────────────────────
# 不必申請，填一個**能收信的位址**就進 polite pool（比匿名的共用池穩定得多，
# 而且他們真的會在你的請求出問題時寄信通知，而不是直接封掉）。
# 缺這個：留在匿名共用池，會被別人的濫用連坐限速。
#
# 說明：https://www.crossref.org/documentation/retrieve-metadata/rest-api/tips-for-using-the-crossref-rest-api/
CROSSREF_MAILTO=

# ── OpenAlex ──────────────────────────────────────────────────────────────
# 同樣不必申請，填信箱就進 polite pool。
# 效果：補上 Crossref 漏掉的撤稿。實測 Wakefield 1998（DOI
#       10.1016/S0140-6736(97)11096-0）Crossref 的 update-to 是 null、
#       relation 是空的，完全看不出已撤稿；OpenAlex 直接回 is_retracted: true。
#       走 DOI 的引用，撤稿偵測唯一的防線就是這裡。
# 缺這個：仍然會查（OpenAlex 免金鑰開放），只是掉出 polite pool。
#
# 說明：https://docs.openalex.org/how-to-use-the-api/rate-limits-and-authentication
OPENALEX_MAILTO=

# ── Unpaywall ─────────────────────────────────────────────────────────────
# 不必申請，網址帶 ?email= 就能用（每日 10 萬次）。
# 效果：`verify_refs.py --fix` 會把開放取用的全文連結寫進 oa_url，前端在引用
#       旁邊多一個「免費全文」——學習者點進去讀得到，而不是撞上付費牆。
# 為什麼 OA 只信它不信 OpenAlex：實測同一個 DOI（10.1001/jama.2022.22625），
#       Unpaywall 給得出 PMC 全文連結，OpenAlex 的 best_oa_location 是 None。
# 缺這個：整段跳過，不報錯也不警告（今天本來就沒有 oa_url 這件事）。
#
# 說明：https://unpaywall.org/products/api
UNPAYWALL_EMAIL=

# ── Europe PMC ────────────────────────────────────────────────────────────
# 這裡沒有欄位，因為它**不需要任何憑證**。它是 NCBI 連續失敗時的 fallback：
# NCBI 偶爾會回 HTTP 500，以前的處理是退避重試三次然後放棄整批。
# 說明：https://europepmc.org/RestfulWebService
